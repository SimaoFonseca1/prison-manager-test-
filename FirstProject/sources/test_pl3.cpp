//
// Created by uni on 23/03/22.
//

//#include "gtest/gtest.h"
//#include "pl3.h"
//TEST(GetPositiveNumber, positiveNumbers) {
  //  int r = getPositiveNumber();
  //  EXPECT_GE(r,0);
//}
//TEST(FilIn, positiveNumbers) {
   // int length = 5;
   // int arr [length];
   // fillInArray(arr, length);
    //printArray(arr, length);
   // for (int i=0; i <length; i++){
      //  SCOPED_TRACE(i); //write to the console in which iteration the error occurred
    //    ASSERT_GE(arr[i],0);
  //  }
//}